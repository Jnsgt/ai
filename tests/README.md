# Tests

This directory contains standalone testing and comparison helpers.

- `compare_outputs.py`: compares multi-case output files generated by different backends

For normal project usage, prefer the packaged CLI in `cldltest/`.
